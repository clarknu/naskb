# 用户决策存档：裁剪口径 + 执行遗漏拍板（2026-08-24）

> 存档规则：原始决策原文追加存档（不修改历史原文）。后续执行以本文件为准。
> 对应台账：design/review/user-decisions-pending.md ｜ 执行入口：iterate（路径 C 批量修正）

## 一、裁剪口径（方法论张力 → 本项目裁剪策略）

1. **权限模型**：保留，不裁剪。理由（用户原话）："这个作为企业内部的非加密资料管理确实非常有用"。
   → 处置：权限点/角色按正式契约对待（当前实现仍单管理员，多用户/多角色为演进 R7-15 路径）。
2. **健康检查/频控/指标**：裁剪掉。
   → 处置：observability-policy 健康端点与指标、resilience-policy 频控 G1-G5 从"演进项"改为"裁剪声明"；
   TDD 架构承诺测试的健康检查维度同步裁剪；发布门禁 7 以 `/api/config/public` + `/api/stats` 200 代替专用健康端点（沿用现状）。
3. **TDD 架构承诺测试（行为承诺）**：要，不裁剪，补齐完整。
   → 按 tdd-build §2.6 矩阵补齐（幂等/并发/缓存命中失效/外部依赖失败/事务回滚/事件发布/审计日志/权限绕过/超时降级；健康检查维度按上条裁剪）。
4. **E2E 门禁**：要。浏览器 E2E 用**全局 Playwright MCP server**（本机 `C:\Soft` 下；换机器用该机器自己的配置）。
   → 处置：新增 integration E2E 旅程规格（design/07-tdd/integration/），执行走全局 Playwright MCP；release 门禁 2 更新为该项目路径。

## 二、执行遗漏拍板（设计-代码差异）

| # | 事项 | 拍板 | 说明 |
|---|------|------|------|
| D-01 | 一致性报告端点（/api/sources/{sid}/report 死代码） | **要做**（接回） | 恢复并定义一致性报告语义 |
| D-02 | /api/folder 端点（孤儿匿名前缀） | **要做** | 目录级描述端点 |
| D-03 | 匿名白名单 | **干掉**——"必须都得是有我的身份的才可以" | 去除匿名只读；全部端点需 token（保留 /api/config/public、/api/docs 匿名作启动引导，待确认） |
| D-05 | 深度分析关闭后存量 chunk | **清理掉** | PATCH deep=false 时删除该来源 level='chunk' 向量行（UI 加确认提示） |
| B-02 | MCP 直链认证 | **不要 Token**——"直链就不用 Token 了，设定一些外围网关层的 IP 约束和限制策略" | 安全边界=网络/网关层（IP 约束），API 层直链不认证；设计资产固化此边界 |
| B-03 | MCP 三工具（kb_list_sources/kb_list_tree/kb_get_file_url） | **要接**（用户判断有价值："我觉得其实是要的"） | 注册/接线并实现未完成部分 |

## 三、发布节奏

- 默认**不发布**。用户明确给出发布指令时：把当前全部工作做完 → 按指令发布到指定环境（发布动作按 release-management 门禁）。

## 四、权益说明

- 以上为 2026-08-24 用户的原文意图记录；execute 中如与本文冲突以本文为准。
