# 需求积压登记册（Requirements Backlog）—— `<项目名>`

> **用途**：登记"随时想到/接收、但不适合当下迭代或 Paper"的**未排期研发需求**，作为**中远期**暂存与孵化池。
> 状态机：`捕获 → 暂存中远期 → 回顾 → 细化中 → 充分待做 → 纳入主链/走 iterate`；任一状态可转 放弃/长期搁置。
> **区别于（分记，不混页）**：`docs/tech-debt-register.md`（技术债登记册：**结论已定、主动推迟**的设计债）｜ `docs/lessons-learned.md`（工程教训，事后复盘）｜ `design/CHANGELOG.md`（按轮次变更流水）。
> **铁律**：登记 ≠ 排期。登记只是把它**钉住**，明确"何时/在什么条件下才轮到它"。判定合格线=**将来看到能直接接上、不用重新决策**（缺"关联链路/触发条件"的登记视为无效）。
> **管理方**：`requirements-backlog` 技能（见 `.agents/skills/sfds/skills/requirements-backlog/SKILL.md`）；本登记册只由它读写，**不**涉及 `design/`、`src/`、`tests/` 资产。

---

## 登记表

| 编号 | 状态 | 登记日期 | 标题 | 来源 |
|------|------|----------|------|------|
| <!-- RB-001 --> | 🔵 暂存中远期 / 🟢 回顾 / 🟡 细化中 / 🟠 充分待做 / ✅ 已纳入 / ⚪ 放弃 / ⚫ 长期搁置 | <!-- 日期 --> | <!-- 一句话主题 --> | <!-- 谁的什么时刻/哪次对话 --> |

---

## RB-xxx｜<标题：一句话主题>

### 原始描述（不解读、不改写）

> <需求原话，保留原文。>

### 来源

- 记录时刻：<!-- YYYY-MM-DD HH:mm -->
- 说出/收到：<!-- 谁 / 哪次对话 / 哪个词 -->

### 细化记录（细化中追加）

> **背景**：<为什么会有这个想法、要解决什么问题。>
> **范围**：<在不在当前项目域内；是否新增域。>
> **约束**：<技术/合规/时间等硬约束。>
> **可验收口径**：<做成什么样算成；避免"更好用"这类模糊目标。>

### 关联链路（预判落点 + 触发条件）

- 预判落点：<!-- 大致落在哪些层/端/域（不必精确，但要能指认出大致归属）。如 后端 API / 前端页面 / ER / 新域 -->
- 触发条件：<!-- 什么信号出现就该排期；如"某依赖升级 / 某功能接入 / 用户两次提出"。 -->

### 细化充分判定（默认从严，§三 5 条）

- [ ] 1. 有明确目标用户 + 价值主张
- [ ] 2. 边界清晰（在/不在当前域，是否新增域）
- [ ] 3. 有可验收口径
- [ ] 4. 有预判落点
- [ ] 5. 无重大不确定阻碍

> 全部满足才升"充分待做"；否则停在"细化中"。

### 状态流水

| 日期 | 状态 | 说明 |
|---|---|---|
| <!-- 登记日 --> | 🔵 捕获 → 暂存中远期 | <!-- 捕获，仅记录不细化 --> |
