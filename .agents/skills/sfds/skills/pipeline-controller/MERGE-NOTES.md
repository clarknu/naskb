# MERGE-NOTES · pipeline-controller 修订记录（CASE-008）

> 单源修订（非三方合并）：基座 = global/pipeline-controller@2026-08-23（v1，85 行）
> 依据：CASE-008 实测缺陷诊断 + 需求方两点补充（关键节点前推 / 确认双通道）→ R1-R4
> 产出：v2.0.0-arb.1

## 修订对照表

| # | 位置 | v1 现状 | v2 修订 | 对应 |
|---|---|---|---|---|
| 1 | 新增首节 | 无任何非阻塞约束 | **最高铁律：主对话零阻塞**——编排器永不内联执行、派发即结束回合回执、违规症状自查 | R1 |
| 2 | 新增"执行者模型"节 | "分析任务丢给子代理"，执行无载体规定 | 三分表：分析=只读子代理并行 / 执行+验证=后台子代理内持写锁串行 / 编排器=纯调度员；**写锁由后台子代理持有，主对话永不持有** | R2 |
| 3 | 新增"前后台通信协议"节 | 无 | 事件枚举 PROGRESS/NEED-INPUT/DONE/BLOCKED/FAILED；时间线播报规则；确认双通道（置顶提示→前台作答→转发后台，不中断不重启）；阻塞型确认持锁标注与取消让路 | R3a/R3b |
| 4 | 新增"任务语义"节 | 上线规则仅"默认不含 deploy" | 「启动发布」等指令默认异步语义；长闭环拆段、每段独立新鲜度门/commit/事件流 | R4 |
| 5 | 核心状态机图 | 执行中为单一节点 | 标注"后台子代理·全局唯一"+ PROGRESS/NEED-INPUT 分支 | R2/R3 |
| 6 | 关键规则 | 规则 1~7 | 新增规则 0（零阻塞优先，冲突时压倒一切）；规则 1 补"写锁由后台子代理持有"；规则 3 标注验证在后台进行；规则 6 接异步语义 | R1/R2/R4 |
| 7 | 会话识别 | 两类话语 | 增加第三类：对未决 NEED-INPUT 的答复 → 转发或取消，不当作新任务 | R3b |
| 8 | 一致性护栏 | 三条 | 增加零阻塞两条自查护栏 | R1 |
| 9 | frontmatter | 无 version/lineage | +version: 2.0.0-arb.1、lineage（origin: arb-hub, case: CASE-008）、description/whenToUse 更新 | 血缘规范 |

## 未改动项（明确保留）

触发方式（严格 opt-in 手势）、激活契约、退出信号、新鲜度门机制、熔断、提交纪律、跨会话持久化、bootstrap 流程——实测均工作正常，CASE-008 缺陷不在这些部分。

## 配套变更

- `templates/PIPELINE.md`：执行中区改为表格（含最新节点）；新增「🔔 待确认」区与「📡 事件日志」区；运行规则第 6 条改异步语义、新增第 8 条（零阻塞与事件回推）。
- `scripts/lock.ps1`、`scripts/freshness.ps1`：无需改动（锁语义不变）。

## 遗留事项

1. 后台子代理的事件输出依赖 DSH 的消息转发能力（send_message / job_output），L2/L3 测试时需实证"NEED-INPUT 置顶 + 答复转发"在真实会话中的时延表现；
2. v1 的 whenToUse 提到"夜里自动消化队列"，与零阻塞兼容，保留于 description；
3. 若 L3 沙盒推演发现编排器无法可靠识别"确认答复 vs 新任务"，考虑给 NEED-INPUT 加显式应答格式（如"T-001: 放行"）作为兜底。
