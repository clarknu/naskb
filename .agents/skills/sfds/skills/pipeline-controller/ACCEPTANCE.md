# ACCEPTANCE · pipeline-controller 仲裁版 v2.0.0-arb.1

> 验收日期：2026-08-23

## L1 静态验收 —— ✅ 通过

| # | 检查项 | 结果 |
|---|---|---|
| 1 | R1-R4 全部落入正文（零阻塞铁律/执行者三分/前后台协议/异步语义各有独立章节） | ✅ |
| 2 | 规则 0 明确零阻塞压倒一切，无条款冲突残留 | ✅ |
| 3 | 事件类型五枚举完整，NEED-INPUT 含问题/选项/影响面/持锁状态四要素 | ✅ |
| 4 | 会话识别覆盖三类话语，确认答复不误判为新任务 | ✅ |
| 5 | 血缘头齐全（origin: arb-hub, case: CASE-008, base 指向全局 v1） | ✅ |
| 6 | 模板同步：PIPELINE.md 新增 🔔待确认/📡事件日志区，运行规则第 8 条 | ✅ |
| 7 | 触发方式保持严格 opt-in（disable-model-invocation: true 未动） | ✅ |
| 8 | scripts/lock.ps1、freshness.ps1 未改动（锁语义不变） | ✅ |

## L2 加载测试 —— ⏳ 待执行

步骤：临时复制到全局 `~/.agents/skills/pipeline-controller-arb/`（或直接替换前先并排测试），以 `/pipeline-controller-arb` 手势激活，验证：激活回执、任务派发后回合立即结束、事件时间线播报。
**注意**：本技能为编排器类，L2 建议直接在低风险真实项目（非生产）做一次小任务全流程。

## L3 沙盒推演 —— ⏳ 待执行

重点推演：
1. "启动发布"指令 → 验证派发后主对话立即交还（CASE-008 原缺陷回归测试）；
2. 执行期插入新问题 → 验证并行分析不被拒收；
3. NEED-INPUT → 前台置顶 → 自然语言作答 → 后台继续（时延与识别准确性）；
4. 「先取消 T-xxx」→ 回滚释放锁。

## L4 真项目试点 —— ⏳ 未开始

建议：zy-ai-consult 重放原始失败场景（"启动发布，我打算看效果挨个提意见"），验收标准 = 主对话全程可输入 + 用户能看到阶段时间线 + 确认往返成功。
