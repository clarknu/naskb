# 部署配置分类策略（Deployment Config Guide）—— `<项目名>`

> **单一真相源**：本文件是发布/部署涉及的「需要读写的资料、需要访问的密钥、需要遵循的逻辑」的**唯一权威/仲裁**。
> **每次部署（`release-management`）必须先读本文件**，再按「分类 → key → credentialctl」取对应配置。
> 配套方法论：`release-management`（怎么发布）· `credential-management`（密钥/配置库）· `deployment-principles`（部署总原则：环境拓扑/中间件放置/网络分层）。
> **⚠️ 本文件是模板（可填骨架）**：只规定「记哪几类、怎么用」，**值由项目填**，不含任何实例值。机密一律以 `<CREDENTIAL:proj/key>` 引用，真值进密钥库；**非密配置值**（IP/域名/地址/端口/用户名/库名/URL/路径/模型名）填在本文件，**不硬编码明文机密**。

<!-- ══════════════════════ ① 环境段对照 ══════════════════════ -->

## ① 环境段对照

| 凭据环境段 | 发布层级 | 含义 | 本项目落地 |
|---|---|---|---|
| `local` | 本地开发部署 | 本机 + 开发联调机，日常开发 + 本地测试 | <!-- 项目填：本机/联调机地址、是进程还是容器 --> |
| `alpha` | 线上开发部署（dev） | 云端研发验收，与线上同构 | <!-- 项目填：是否落地、地址/域名 --> |
| `beta` | 线上预发部署（pre-prod） | 真实链路验收 / 上线演练 / 迁移预演 | <!-- 项目填：是否落地、地址/域名 --> |
| `release` | 线上正式环境（prod） | 正式对外 | <!-- 项目填：地址/域名 --> |

- 命名对照：`alpha`/`release` 是密钥库里的环境段；发布方法论层级代号是 `dev`/`prod`，二者一一对应。
- 晋升路径 `local → alpha → beta → release`；晋升 = 在目标环境重跑同一套发布流程。
- **线上基础设施模型**：`<!-- 项目填：线上各环境是否同一主机、容器编排、共享库位置（单主机/多主机）、与哪些项目共宿主、是否共用 Docker 网络 -->`。

<!-- ══════════════════════ ② 分类维度 ══════════════════════ -->

## ② 分类维度

凭据/配置按两个正交维度分类，决定「取名」与「从哪取值」：
1. **环境作用域**：`共享`（环境中性，**不加前缀**）｜ `环境作用域`（随环境不同，**加 `{env}_` 前缀**）。
2. **资源 / 组件类型**（kind）：按凭据的**真实用途/性质**取名（服务器地址、登录账号、API Key、令牌、数据库等），**不限 SSH**。

> 关键判断：**只有「随环境不同」的东西才加环境前缀**；环境中性、部署必用的东西一律不加前缀。

<!-- ══════════════════════ ③ 每个 key 类别 ══════════════════════ -->

## ③ 每个 key 类别

### 环境中性（共享）——不加前缀
| 项 | key（名称模式） | 说明 | 机密/非密 |
|---|---|---|---|
| `<服务/提供方>` 密钥 | `{service}_api_key` / `{provider}_access_key_id`+`_secret` | `<项目填：全环境共用、按服务取名>` | 机密 |
| 线上主机（线上共用） | `online_server`/`online_user`/`online_password`/`online_port` | 所有线上环境共用的托管主机 | server/user 非密；password 机密 |
| 数据库（线上共享库） | `db_host`/`db_port`/`db_name`/`db_user`/`db_password` | 线上环境共用库 | host/port/name/user 非密；password 机密 |
| `<服务>`平台地址 | `{service}_base_url` | 共享平台本体地址 | 非密 |
| `<服务>`平台 API Key | `{service}_api_key` | 共享平台鉴权 Key | 机密 |
| 其它 | `{service}_*`（按服务命名的密钥/口令） | `<项目填>` | 按性质 |

> **开发 Key 与 运行 Key 分离**：`{service}_api_key` 记的是**项目研发/测试/演示（自主运行）**用 Key；**AI Coding（开发/编码）**用的 Key 是**另一把**，**不进项目密钥库**（由编码 Agent/平台各自持有），**严禁混用**。

### 环境作用域——加 `{env}_` 前缀
| 项 | key | 说明 | 机密/非密 |
|---|---|---|---|
| 服务器/登录 | `{env}_server`/`{env}_user`/`{env}_password`/`{env}_port` | 该环境主机 | server/user/port 非密；password 机密 |
| 随环境独立部署的组件 | `{env}_{svc}_server`/`{env}_{svc}_user`/`{env}_{svc}_password`/`{env}_{svc}_db_password`… | 每环境一份独立实例 | 按性质 |
| 本地数据库 | `local_db_*` | 本地研发专用库 | host/port/name/user 非密；password 机密 |
| 共享平台上的每环境引用 | `{env}_{svc}_project`/`{env}_{svc}_app_id`/`{env}_{svc}_workflow_id` | 平台共享一份、环境各占一个对象 | 非密 |

> 环境段取 `local`/`alpha`/`beta`/`release`；**线上主机（线上共用）用 `online_*`**。

<!-- ══════════════════════ ④ 机密 vs 非密 + 部署步骤→密钥库 Key ══════════════════════ -->

## ④ 机密 vs 非密 + 「部署步骤 → 密钥库 Key」

> **边界（铁律）**：**密钥库只存机密**（口令 / API Key / 令牌 / 密钥材料）；**非密部署配置**（IP、域名、地址、端口、用户名、库名、URL、路径、模型名）**放本文件**，**不进密钥库**。本文件同时维护「部署步骤 → 密钥库 Key」对照表，**用到哪个 Key 才去密钥库取**（绝不提前/批量泄露）。

**进密钥库的机密**（示例模式）：`{service}_api_key`、`{provider}_access_key_secret`（配 `_id`）、`{service}_db_password`、`redis_password`、`online_password`、`{env}_password`、`{env}_{svc}_api_key`、`{svc}_*` 密钥/令牌/口令。

**进本文件的非密配置**：`{service}_host`/`{service}_port`/`{service}_user`、`{env}_db_name`/`{env}_db_user`、`online_server`/`online_user`/`online_port`、`{env}_server`/`{env}_user`/`{env}_port`、`{service}_base_url`、`{env}_{svc}_project`/`_app_id`/`_workflow_id`、各域名/地址/路径/URL。

> **设计资产/对外产物必须落库**：凡"开发出来并部署到外部"的东西（自托管应用/流程 DSL/模型提供方/插件配置/MCP 服务器/协议层/部署拓扑/.env 骨架），**代码库副本必须 commit**、且只存 `<CREDENTIAL:proj/key>` 引用，真值进密钥库（见 `release-management` §八 #11）。

### 部署步骤 → 密钥库 Key 对照表（用到才取）
| 步骤 | 需要的密钥库 Key |
|---|---|
| `<!-- 步骤 1（如基础/域名/证书) -->` | `<!-- 如 provider_access_key_id + _secret -->` |
| `<!-- 步骤 2（如建库) -->` | `<!-- 如 db_admin_password -->` |
| `<!-- 步骤 3（如中间件) -->` | `<!-- 如 redis_password -->` |
| `<!-- 步骤 4 … -->` | `<!-- … -->` |

<!-- ══════════════════════ ⑤ 部署目标与关口 ══════════════════════ -->

## ⑤ 部署目标与发布门禁（命令映射）

- **部署目标与脚本**：
  - `local`：`<!-- 项目填：本机/联调机 部署脚本 -->`
  - `online`：`<!-- 项目填：线上部署脚本（单主机/多主机） -->`
- **发布门禁**（对应 `release-management` §二，逐项映射项目命令）：
  | 门禁 | 本项目命令/产物 | 说明 |
  |---|---|---|
  | 1 全量测试绿 | `<!-- 项目填 -->` | 0 失败 |
  | e2e 绿 | `<!-- 项目填 -->` | |
  | ORM-DB 无差异 | `<!-- 项目填 -->` | |
  | 迁移预演 | `<!-- 项目填 -->` | |
  | 密钥齐全 | `<!-- 项目填：远程 .env chmod 600 由部署脚本生成、不入库 -->` | |
  | CHANGELOG 更新 | `<!-- 项目填 -->` | |
  | 健康检查 | `<!-- 项目填：/health 返回 ok -->` | |
  | 目标环境全量回归 | `<!-- 项目填：API 全端点回归 + 协议层冒烟 + 关键旅程冒烟 + 环境对比 -->` | |
  | 架构契约校验（可选） | `<!-- 项目填 -->` | |
- **版本规范**：SemVer `vX.Y.Z`；tag `vX.Y.Z` / 预发布 `vX.Y.Z-beta.N`；main 直发 + tag；禁止在 tag 上改代码。
- **线上安全红线**：线上变更只通过发布流程发生；迁移闸门；存量库拒绝自动迁移；切换必须有回退；发布基于已提交版本。

<!-- ══════════════════════ ⑥ 部署蓝本 ══════════════════════ -->

## ⑥ 部署蓝本（已执行做法，后续照抄）

> 已实际执行的具体做法，后续部署按此照做、减少每次登机分析；机密一律引用。

- **主机与基础设施**：`<!-- 项目填：主机/OS/CPU 内存/容器运行时/镜像源/证书 -->`
- **公共中间件（宿主机分层 + 宿主端口）**：
  - 数据库（PG）：`<!-- 项目填：容器 or 原生、端口、库/用户、挂载、为何、如何映射到宿主端口 -->`
  - Redis：`<!-- 项目填：原生、bind、requirepass、持久化 -->`（原生实例走原生 Host 绑定）
  - 其它 MQ/对象存储/向量库：`<!-- 项目填 -->`
- **网络访问（deployment-principles §三）**：
  - **一个项目一个 Docker 网络**：`<!-- 项目填：项目网络名、容纳哪些环境/服务，服务名互访 -->`
  - 公共中间件经**宿主端口**访问：`<!-- 项目填：宿主端口 → 中间件容器/原生绑定 -->`
  - **Nginx 层统一对外**：`<!-- 项目填：Nginx 反代表（子域 → 内网端口）、TLS 终结、WebSocket 透传 -->`；对外只经 Nginx/域名映射（或隧道兜底）。
- **服务间通信**：容器服务间用 compose 网络**服务名**解析；访问宿主机原生中间件用 `host.docker.internal`（host-gateway）。
- **暴露与边界**：业务容器/宿主端口**不直接暴露公网**；对外只经 Nginx（或隧道兜底）。
- **密钥/凭据归置**：`<!-- 项目填：哪些进密钥库、哪些进本文件 -->`。

<!-- ══════════════════════ ⑦ 坑与注意 + 下次部署 ══════════════════════ -->

## ⑦ 坑与注意 + 下次部署

### 坑与注意（每次部署踩到的坑）
| 坑 | 现象 / 一句话 | 处置 / 预防 |
|---|---|---|
| `<!-- 部署坑 1 -->` | `<!-- 现象 -->` | `<!-- 处置 -->` |
| `<!-- 部署坑 2 -->` | `<!-- 现象 -->` | `<!-- 处置 -->` |

> 需要引用 `release-management` §八 的通用纪律（UTF-8/SSH 长连接/禁线上 clone/服务名解析/设计资产落库等），逐条对照本项目是否有对应坑。

### 下次部署怎么做
- `<!-- 项目填：按上面 ①-⑥ 依次核对：环境段 → 分类 → 取 key → 按门禁 → 部署蓝本 → 回填坑 -->`
- 变更同步：更新本文件 + 设计资产库（`design/assets/<域>/`）+ `CHANGELOG.md` + 随代码 commit。
- 配置变更只改本文件（单真相源）；密钥变更走 `credentialctl`，不进本文件。
