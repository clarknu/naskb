---
name: iterate
version: 1.0.0
description: >
  功能迭代 + Bug 修复 + 联调批量修正的统一级联引擎。当用户对当前状态表达否定、不满或修正意图时触发，
  覆盖报错修复、字段/页面/设计调整、新增功能、需求与实现不一致、联调多问题批量修正等场景。
  三条路径：A 功能迭代 / B 诊断修复 / C 联调修正（批量）。
triggers:
  # ── 显式报错 / 异常（→ 路径 B）──
  - 报错
  - 500
  - 崩溃
  - bug
  - 异常
  - 出错了
  - 测试失败
  - 测试挂了
  - 挂了
  - 错误
  - 诊断
  - 问题排查
  - 根因分析
  # ── 否定判断（→ 视上下文 A/B）──
  - 不对
  - 不应该
  - 有问题
  - 问题
  - 少了
  - 缺了
  - 没有
  - 多了
  - 不正常
  # ── 修正 / 调整命令（→ 视上下文 A/B）──
  - 改一下
  - 修改
  - 调整
  - 重构
  - 换成
  - 替换
  - 删掉
  - 去掉
  - 修复
  - 更新
  # ── 新增意图（→ 路径 A）──
  - 新增
  - 加一个
  - 添加
  - 支持一下
  # ── 需求-实现差距（→ 路径 A）──
  - 设计里还没
  - 代码没体现
  - 需求里说了
  # ── 联调修正场景（→ 路径 C）──
  - 联调
  - 联调修正
  - 集成调试
  - 前后端对接
  - 接口对不上
  - 不匹配  # → 单点 B / 批量 C（见派发表）
  - 遗漏
  - 未同步
  - 对不上
  - 整体检查
lineage:
  origin: arb-hub
  sources:
    boxing:        {sha256: 3665d6c3ebab}
    zy-iot-ai:     {sha256: 949efb39d412}
    zy-ai-consult: {sha256: 37bd78e52cf1}
  rulings: [R-002]
---

# Skill: iterate — 迭代变更编排器

> 功能迭代 + Bug 修复 + 联调批量修正的统一级联引擎。以同步螺旋协议（SFDS §10.3b）为核心编排机制，
> 覆盖从"想加一个功能"到"联调发现一批问题"的全部变更场景。

---

## ⛔ 强制两阶段铁律（红线）

> **本 skill 分两阶段执行，阶段一完成后必须停止并等待用户确认，不得自动进入阶段二。**

```
阶段一：分析 + 出计划
    │
    ├── 路径 A：FA1 → FA2 → FA3 → C1（产出：级联修正计划）
    ├── 路径 B：FB1 → FB2 → C1（产出：级联修正计划）
    ├── 路径 C：FC1 → FC2 → FC3 → C1（产出：批量联调修正计划）
    │
    └── ⛔ 强制停止，等待用户确认。不得自动进入阶段二。
            │
            ├── 用户确认 → 进入阶段二
            ├── 用户修改 → 回到阶段一调整计划
            └── 用户拒绝 → 终止
                    │
阶段二：执行 + 验证
    │
    └── C2（逐步骤执行）→ C3（验证闭环）
```

### 唯一例外：用户明确要求合一步

如果用户在触发本 skill 时**显式声明**以下任一表述，可以跳过阶段一的停止点，分析完成后直接执行：

- "不需要确认，直接改"
- "一步完成"
- "分析完直接执行"
- "不用等我确认"
- "auto" / "yolo" / "--force"

> **默认行为永远是两步走。** 如果用户在阶段一完成、看到计划后说"好的，开始执行"——那是正常的确认流程，不是合一步声明。
> 区分点：**合一步声明必须在触发 iterate 的同一句话里出现。**

---

## 适用场景

| 场景 | 模式 | 典型触发语 |
|------|------|-----------|
| 加字段、加端点、加页面、改工作流、改业务规则 | **路径 A：功能迭代** | "给订单加一个取消原因字段"、"用户注册流程要加手机验证" |
| 需求变更、原始输入更新 | **路径 A：功能迭代** | "合同附录里说了要支持多设备绑定，设计里还没体现" |
| 设计-实现偏差、代码与设计不一致 | **路径 A 或 B** | 视偏差方向而定 |
| API 500、测试失败、启动崩溃、数据错误 | **路径 B：诊断修复** | "订单接口返回 500"、"3 个测试挂了" |
| 行为不符合预期、权限异常 | **路径 B：诊断修复** | "已取消的订单还能编辑"、"普通用户看到了管理后台" |
| **联调中发现多个问题（混合类型：功能缺失 + 接口不匹配 + 页面错漏）** | **路径 C：联调修正（批量）** | "联调发现 3 个问题：设备分组数量不对、搜索失效、地域字段语义不符" |
| 联调后整体检查设计-实现一致性 | **路径 C：联调修正（批量）** | "整体检查一下，找出遗漏或不匹配的地方" |

---

## 核心设计理念

### 三条路径，一个引擎

```
用户输入
    │
    ├── "我想加/改 X" ──→ 路径 A：功能迭代
    │                        │
    │                        ├── FA1: 澄清变更意图
    │                        ├── FA2: 定位插入点（哪个 §8.X）
    │                        ├── FA3: 上溯检查（上游需补上下文吗？）
    │                        └── → 进入共享级联引擎 C1
    │
    ├── "X 出问题了" ──→ 路径 B：诊断修复
    │                        │
    │                        ├── FB1: 复现与收集证据
    │                        ├── FB2: 根因追溯（最早偏差步骤）
    │                        └── → 进入共享级联引擎 C1
    │
    ├── "联调发现 N 个问题" ──→ 路径 C：联调修正（批量）
    │                              │
    │                              ├── FC1: 收集问题清单（一次联调暴露的全部问题）
    │                              ├── FC2: 逐个分类（A 功能修正 / B 诊断修复）→ 定位 §8.X
    │                              ├── FC3: 合并级联计划（多起点、去重共享环节）
    │                              └── → 进入共享级联引擎 C1
    │
    └── ⛔ 阶段一结束，强制停止，等待用户确认
              │
              └── 用户确认后：
                       ├── C2: 级联执行（逐步骤委托专业 skill）
                       └── C3: 验证闭环（测试全绿 + Review 通过）
```

### 与同步螺旋协议的关系

本 skill 是 SFDS §10.3b「同步螺旋协议」的**操作化实现**。同步螺旋定义了五步法：

> 标记真相源 → 上溯 → 下传 → 扩散迭代 → 收敛

本 skill 的路径 A 直接映射这五步；路径 B 通过根因追溯自动发现真相源后，汇入同一螺旋。

---

## 模式分发

### 自动判断

根据用户输入的关键词自动选择路径：

| 用户说… | 判定 | 路径 |
|---------|------|------|
| "加一个…"、"新增…"、"添加…"、"支持…" | 增量变更 | A |
| "改一下…"、"修改…"、"调整…"、"重构…"、"迭代…" | 存量修改 | A |
| "修复…"、"更新…" | 视范围而定 | 数量 1-2、单文件范围 → 默认 **B**；涉及多端多文件 → 向用户确认走 **A** 或 **C** |
| "需求里说了…但设计/代码没体现" | 需求→设计 gap | A |
| "报错"、"500"、"崩溃"、"挂了"、"不正常"、"bug" | 异常/缺陷 | B |
| "为什么…"（描述意外行为） | 行为不符合预期 | B |
| "测试失败了"、"N 个 red" | 测试失败 | B |
| **"联调"、"联调修正"、"集成调试"、"前后端对接"** | 联调场景（批量） | **C** |
| **单个/局部"不匹配"、"遗漏"、"未同步"、"对不上"、"接口对不上"（单点问题）** | 单点问题 | **B**（诊断修复） |
| **多个问题且源自联调/跨端对接** | 联调场景（批量） | **C**（批量） |
| **"整体检查" + "找出问题/遗漏/不一致"** | 联调场景（批量，先走查后修正） | **C** |

> 如无法自动判定，向用户确认："这是要加新功能/改设计（迭代），修单个问题（排查），还是联调批量修正？"

---

## ═══════════════════════════════════════
## 阶段一：分析 + 出计划
## ═══════════════════════════════════════

> 阶段一产出**级联修正计划**，然后强制停止，等待用户确认。

---

## 路径 A：功能迭代

> **适用**：用户主动发起设计或功能的增量/修改。真相源由用户声明，不需要"发现"。

### FA1 — 澄清变更意图

**目标**：把用户的自然语言描述转化为可执行的变更规格。

**执行步骤**：

1. **理解意图**：用户在说什么？要改什么？为什么？
2. **判断范围**：这是单点改动还是连锁改动？
   - 单点："给订单表加一个 `cancelReason` 字段"
   - 连锁："用户注册流程要支持手机验证"（涉及工作流、ER、API、页面）
3. **输出变更摘要**：

```markdown
## 变更意图
- **描述**：{一句话}
- **来源**：用户主动提出 / 原始需求追溯 / 设计审查发现
- **类型**：新增功能 / 修改已有 / 删除 / 重构
- **初步判断影响域**：{涉及哪些领域}
- **初步判断影响层**：§8.1 / §8.2 / §8.3 / §8.3b / §8.4 / §8.5 / §8.6 / §8.7
```

4. **存档**：如果变更是由原始需求驱动的，追加到 `design/01-raw-input/{domain-slug}.md`（标注时间戳 + 变更原因，不修改历史原文）。

### FA2 — 定位插入点

**目标**：确定变更在 SFDS 流水线中从哪一步开始。

**定位方法**：

| 变更内容 | 最早插入步骤 |
|---------|------------|
| 加/改业务规则、状态转换、权限点 | §8.1 工作流 |
| 加/改实体、字段、约束、关系 | §8.2 ER |
| 加/改 API 端点、参数、响应结构 | §8.3 API 设计 |
| 加/改架构策略、缓存、可靠性 | §8.3b 架构设计 |
| 加/改页面、组件、导航 | §8.4 页面设计 |
| 仅改测试用例 | §8.5 TDD |
| 仅改实现代码（不改设计） | §8.6 / §8.7 |
| 需求文档补充（不改下游） | raw-input 存档 |

**输出**：

```markdown
## 插入点
- **最早变更步骤**：§8.X
- **定位依据**：{为什么从这里开始}
```

### FA3 — 上溯检查

> **核心问题**：如果我从 §8.X 开始改，上游有没有缺失的上下文？
> 这个变更依赖的概念/字段/规则，在上游是否已经定义清楚了？

**上溯规则**（逐层向上，每层问一个问题）：

| 当前插入点 | 上溯检查 |
|-----------|---------|
| §8.7 前端代码 | 页面设计有对应页面/组件吗？API 设计有对应端点吗？ |
| §8.6 API 代码 | API 设计有对应端点吗？ER 有所需字段吗？工作流有对应业务规则吗？ |
| §8.5 TDD | API/架构/页面设计是否已经稳定？——通常 TDD 跟着设计走，不独立启动 |
| §8.4 页面设计 | API 有所需端点吗？ER 有页面要展示的字段吗？工作流有对应的用户操作吗？ |
| §8.3 API 设计 | ER 有所需字段和约束吗？工作流有对应的业务操作和权限点吗？ |
| §8.3b 架构设计 | API 设计的非功能需求明确吗？工作流的并发/可靠性要求是什么？ |
| §8.2 ER | 工作流中有对应的业务节点产生/消费这些实体吗？原始需求中有描述吗？ |
| §8.1 工作流 | 原始需求中有对应的功能描述吗？ |

**上溯终止条件**：
- 该层已有完整的定义 → 停止，不需要继续向上
- 该层缺少定义 → **插入点上移**到这一层
- 上溯到 raw-input → 追加原始交互记录

**上溯示例**：

```
用户："给订单列表 API 加一个 cancelReason 字段"
↓
FA2 定位：表面上是 §8.3（API 设计，改响应结构）
↓
FA3 上溯检查：
  §8.3 → §8.2：ER 的 Order 实体有 cancelReason 字段吗？
    ├── 有 → 插入点确认为 §8.3，上溯停止
    └── 没有 → 插入点上移到 §8.2，继续上溯
        §8.2 → §8.1：工作流的"取消订单"节点有产生取消原因吗？
          ├── 有 → 插入点确认为 §8.2
          └── 没有 → 插入点上移到 §8.1
```

**输出**：

```markdown
## 上溯结果
- **最终插入点**：§8.X（可能是调整后的）
- **上溯路径**：{逐层检查记录}
- **上游补充需求**：{哪些层需要补什么}
```

---

## 路径 B：诊断修复

> **适用**：用户报告问题——行为异常、报错、测试失败等。需要从症状追溯到根因。

### FB1 — 复现与收集证据

1. **理解问题**：预期行为 vs 实际行为
2. **复现**：构造复现步骤，确认可复现
3. **收集证据**：日志、响应体、堆栈、数据库状态、测试结果

**启动服务（如需要）**：从项目 AGENTS.md 读取启动命令。启动前检查编译和数据库迁移。

### FB2 — 根因追溯

> **核心问题**：这个 bug 的种子是在 SFDS 流水线的哪一步种下的？
> **不是**"哪里改一行代码就好了"，而是"哪个环节最先出现偏差"。

**追溯方法——向上追问法**：

从症状出发，逐层追问："如果上游早就有这条规定/定义，这个 bug 还会发生吗？"

| 回答 | 结论 |
|------|------|
| "不会" → | 根因在上游，继续向上追溯 |
| "会，因为上游已经写清楚了我只是在代码里漏掉了/写错了" → | **根因在当前层，停止** |

**⚠️ 防误判铁律**：代码缺失 ≠ 实现 bug。如果代码没写某字段，但设计文档也没定义该字段 → 根因在设计层，不是代码层。

**症状→步骤分类表**：

| 症状 | 最早偏差步骤 |
|------|------------|
| 功能完全缺失 | 追溯链上第一个"没有"的位置（需求→工作流→ER→API→页面→代码） |
| 业务逻辑错误 | §8.1（工作流缺失规则）或 §8.3（API 设计偏差） |
| API 字段缺失/多余/类型错误 | §8.3（API 设计未定义）或 §8.6（DTO 偏差） |
| 数据库约束/查询报错 | §8.2（ER 缺失约束）或 §8.6（迁移未应用） |
| 页面不存在或入口缺失 | §8.4（页面设计缺失）或 §8.7（前端代码缺失） |
| 测试失败 | §8.5（测试写错）或 §8.6（实现偏差）或 §8.3（设计本身有矛盾） |
| 前后端对接失败 | §8.7（前端调用错）或 §8.6（路由/DTO 不一致） |
| 数据流断裂 | 追溯链上第一个"有产出但下一环节未接收"的位置 |
| 编译失败 | §8.6 或 §8.7（代码编译问题） |

**输出**：

```markdown
## 根因分析
- **最早偏差步骤**：§8.X
- **追溯链**：{从症状到根因的逐层追问记录}
- **分类**：API 相关 / 纯前端
- **根因描述**：{具体是什么偏差——设计遗漏 / 设计错误 / 实现偏差 / 配置问题}
```

---

## 路径 C：联调修正（批量模式）

> **适用**：联调/集成调试场景——架构基本完成后，通过前后端对接发现**多个问题**，
> 涉及功能设计、业务逻辑、代码实现等多个环节的混合修正。
> **与路径 A/B 的区别**：A/B 是单问题驱动（一次处理一个变更/一个 bug）；
> C 是**批量驱动**——一次联调暴露的 N 个问题收集成清单，逐个分类后合并成一份级联计划。
> **与 review 的区别**：review 只检查并输出问题清单（不修）；路径 C 检查 + 修正一体。

### FC1 — 收集问题清单

**目标**：把一次联调暴露的全部问题收集成结构化清单，不遗漏。

**执行步骤**：

1. **逐条收集**：用户报出的每个问题 = 清单一行。如果用户说"整体检查"，先执行走查（对照设计资产 vs 实际行为），把发现的每处偏差也列入清单。
2. **每条问题记录**：

```markdown
| # | 问题描述 | 期望行为 | 实际行为 | 涉及环节（初步） |
|---|---------|---------|---------|----------------|
| 1 | 设备分组列表 device_count 恒为 0 | 显示真实设备数 | 聚合未实现 | §8.6 API 代码 |
| 2 | 分组搜索失效 | 按名称过滤 | 前端未传搜索参数 | §8.7 前端代码 |
| 3 | 地域分组语义不符 | 按地域分组 | 设计未定义地域维度 | §8.2 ER / §8.1 工作流 |
```

3. **确认清单完整性**：与用户核对"还有没有其他问题？"——清单闭环后再进入 FC2。**遗漏问题会打断批量修正，务必先确认完整。**

### FC2 — 逐个分类与定位

**目标**：对清单中每个问题单独走分类逻辑，确定它属于路径 A 还是 B，以及根因在哪一层。

| 问题特征 | 判定 | 定位方法 |
|---------|------|---------|
| 功能缺失、行为不符、接口报错、数据错误 | **路径 B 诊断** | FB2 根因追溯（向上追问法 + 症状→步骤表） |
| 需求澄清、设计调整、新功能补充 | **路径 A 迭代** | FA2 定位插入点 + FA3 上溯检查 |
| 设计-实现偏差（代码与设计不一致） | 视偏差方向 | 设计正确 → B（实现 bug）；代码更优 → A（设计更新） |

**输出**：问题清单扩展为分类结果：

```markdown
| # | 问题 | 分类 | 根因层 | 级联链 |
|---|------|------|--------|--------|
| 1 | device_count 恒为 0 | B | §8.6 | §8.6 → §8.5(回归) → §8.8 |
| 2 | 搜索失效 | B | §8.7 | §8.7 → §8.8 |
| 3 | 地域分组语义 | A | §8.2 | §8.2 → §8.3 → §8.3b → §8.4 → §8.5 → §8.6+§8.7 → §8.8 |
```

### FC3 — 合并级联计划

**目标**：把 N 条独立级联链合并成**一份**批量修正计划，去重共享环节。

**合并规则**：

1. **按 §8.X 层排序**：所有问题按根因层从上游到下游排序（§8.1 → §8.8），先改上游再改下游
2. **同层合并**：多个问题根因在同一层 → 合并为该层的一个步骤（如两个 §8.6 问题 → 一个"§8.6 API 代码修正"步骤，列两个变更点）
3. **共享环节只执行一次**：如果多个级联链都经过 §8.5（tdd-build）或 §8.8（tdd-execute），合并为一次全量回归
4. **测试强制注入**：凡属 API 相关范畴的问题（§8.1/§8.2/§8.3/§8.3b/§8.5/§8.6），合并计划中 §8.5 必须为每个问题新增至少一个回归用例；页面问题（§8.7）按规则 #14 加 Web UI 集成测试——**联调批量修正无"纯前端豁免"**
5. **原始输入存档**：联调中产生的需求澄清、行为确认，先追加到 `design/01-raw-input/{domain-slug}.md`（标注时间戳 + 联调上下文），再从对应层开始级联

**输出格式**（在 C1 基础上扩展）：

```markdown
## 批量联调修正计划

> 模式：联调修正（批量）| 问题数：N | 涉及域：{domains}
> API 相关：{是/否}

### 问题清单
| # | 问题 | 分类 | 根因层 |
|---|------|------|--------|
| 1 | ... | A/B | §8.X |

### 合并级联计划
| 序号 | 步骤 | 负责 Skill | 变更内容（关联问题 #） | 备注 |
|------|------|-----------|----------------------|------|
| 1 | §8.X | {skill} | {变更}（#1, #3） | 起点 |
| ... | ... | ... | ... | ... |
| N | §8.5 | tdd-build | 回归用例 × M（#1, #2...） | ⚠️ 每个问题至少 1 个 |
| N+1 | §8.8 | tdd-execute | 全量测试 | 含回归 |
| N+2 | Review | review | 受影响维度复查 | — |

**并行机会**：§8.6 + §8.7 可并行
**已检查无需变更**：{列出跳过但已确认的步骤}
```

### ⛔ 阶段一停止点（同 C1）

批量计划输出后，**必须停止**，等待用户确认。用户可对单个问题调整分类或根因层，调整后回到 FC2 重算该问题的级联链。

---

## 共享级联引擎 — C1（阶段一的最后一步）

> 三条路径在确定变更起点后，汇入 C1 产出级联计划。

### C1 — 级联计划

**目标**：计算从变更起点到 §8.8→Review 的完整修正链路。

**级联链计算规则**（基于 SFDS §8.0 依赖拓扑）：

| 起点 | 级联链（正向下游） |
|------|------------------|
| raw-input | §8.1 → §8.2 → §8.3 → §8.3b → §8.4 → §8.5 → §8.6+§8.7 → §8.8 → Review |
| §8.1 工作流 | §8.2 → §8.3 → §8.3b → §8.4 → §8.5 → §8.6+§8.7 → §8.8 → Review |
| §8.2 ER | §8.3 → §8.3b → §8.4 → §8.5 → §8.6+§8.7 → §8.8 → Review |
| §8.3 API 设计 | §8.3b → §8.4 → §8.5 → §8.6+§8.7 → §8.8 → Review |
| §8.3b 架构设计 | §8.5 → §8.6 → §8.8 → Review |
| §8.4 页面设计 | §8.5 → §8.7 → §8.8 → Review |
| §8.5 TDD | §8.6+§8.7 → §8.8 → Review |
| §8.6 API 代码 | §8.5（⚠️ 回归用例）→ §8.8 → Review |
| §8.7 前端代码 | §8.8 → Review |

> **关于小程序端：** 以上级联链中的 §8.5（tdd-build）和 §8.8（tdd-execute）会根据项目端类型
> 自动检测并执行对应测试阶段。当项目存在小程序端时，§8.5 除 Stage 2（Web）外还产出
> Stage 2b（Mini Program）测试设计；§8.8 除执行 Stage 2 外还执行 Stage 2b。
> 级联链本身不需要显式添加 "§8.7b 小程序前端" 步骤——§8.7（前端代码）覆盖所有端类型。
>
> **前端生成验收契约：** code-gen 完成清单应包含「调用 tdd-execute 执行对应端测试阶段」（按项目端类型映射到对应测试阶段）；验证层技能（tdd-build / tdd-execute）是前端生成的验收方。

**计划输出格式**：

```markdown
## 级联修正计划

> 变更起点：§8.X | 模式：{功能迭代 / 诊断修复}
> API 相关：{是/否}（如果是，§8.5 必须新增 TDD 回归用例）

| 序号 | 步骤 | 负责 Skill | 变更内容 | 备注 |
|------|------|-----------|---------|------|
| 1 | §8.X | {skill} | {具体要做什么} | 起点 |
| 2 | §8.Y | {skill} | {具体要做什么} | — |
| ... | ... | ... | ... | ... |
| N | §8.8 | tdd-execute | 全量测试 | 含回归用例 |
| N+1 | Review | review | 受影响维度复查 | — |

**并行机会**：§8.6 + §8.7 可并行执行（如两者都在级联链中）
**已检查无需变更**：{列出跳过但已确认的步骤}
```

### ⛔ 阶段一停止点

C1 计划输出后，**必须停止**。输出以下确认提示：

```markdown
---
> ⛔ **阶段一完成。** 请确认以上级联修正计划：
> - 回复 **"确认"** / **"开始"** → 进入阶段二执行
> - 回复修改意见 → 调整计划
> - 回复 **"取消"** → 终止
---
```

> **此停止不可跳过，除非用户在触发 iterate 时已声明合一步模式。**

---

## ═══════════════════════════════════════
## 阶段二：执行 + 验证
## ═══════════════════════════════════════

> 阶段二仅在用户**明确确认**阶段一的计划后启动。
> 如果用户确认时附带修改意见，先回到阶段一调整计划，再启动阶段二。

### C2 — 级联执行

**执行规则**：

| # | 规则 |
|---|------|
| 1 | **委托不替代**：每一步的实质性修改必须通过对应的专业 skill 执行。本 skill 只做编排和守卫 |
| 2 | **按序执行**：从起点开始，按依赖顺序逐步骤执行。§8.6 和 §8.7 可并行；如果项目同时有 Web 和小程序端，各自的 §8.7 也可并行 |
| 3 | **TDD 强制注入**：如果变更属 API 相关范畴（§8.1/§8.2/§8.3/§8.3b/§8.5/§8.6），级联链中的 §8.5 步骤必须新增至少一个针对本次变更的回归测试用例。§8.5 会按项目端类型自动分发：Web 端→Stage 2 回归，小程序端→Stage 2b 回归。纯前端功能迭代变更可豁免；**联调 bug 修复不豁免**（见规则 #13，页面问题须加 Web UI 集成测试） |
| 4 | **每步验证**：每步完成后检查产物更新 + CHANGELOG 记录 |
| 5 | **设计是权威源**：如果代码与设计冲突，以设计为准。代码只能回归设计，不可反向覆盖 |
| 6 | **域注册表同步**：如果变更涉及领域边界调整，先更新 `design/domain-registry.js` |
| 7 | **CHANGELOG 必写**：每步变更后在其负责目录的 `CHANGELOG.md` 中记录 |
| 8 | **零变更也记录**：如果某步骤确实不需要修改，在计划中标注"已检查，无需变更"——不可静默跳过 |
| 9 | **批量按合并计划执行**（路径 C）：按 FC3 合并后的计划执行——同层合并、共享环节只执行一次；每个变更点标注问题 #，确保清单逐项闭环 |

**每步执行模板**：

```markdown
### 🔄 Step X/N：§8.X — {步骤名称}

**负责 Skill**：{skill-name}
**输入**：{已就绪的上游产物}
**动作**：{具体变更内容}
**TDD 回归**：{⚠️ 必须新增 / N/A}

---
{skill 执行输出}
---

**产物验证**：
- [ ] {产物文件} 已更新
- [ ] CHANGELOG 已记录
- [ ] {如果是 §8.5} 回归测试用例已新增

**状态**：✅ / ❌（{原因}）
```

**受阻处理**：如果某步需要用户仲裁（如设计歧义），暂停级联，提交用户决策后从受阻步骤继续。已完成步骤的结果保留。

### C3 — 验证闭环

**三个硬性条件**（必须全部满足）：

1. **§8.8 tdd-execute 全绿**（0 失败，含本次新增回归用例 + 所有已有测试，覆盖项目所有端类型的测试阶段）
2. **Review 无新实质性问题**（或仅受影响维度检查通过）
3. **原始问题/变更意图已确认**：
   - 路径 A：变更已生效，功能符合预期
   - 路径 B：原始 bug 不再复现
   - 路径 C：问题清单逐项确认已解决（每个问题 # 有对应修复 + 测试），无遗留项

### C3.1 — 前置脚本校验（进入 Review 前必执行）

级联执行完成后、启动 Review 之前，必须运行以下项目脚本确认无新增偏差：

| 检查 | 脚本 | 通过标准 |
|------|------|---------|
| A — ER↔ORM 对齐 | `scripts/validate-er-orm-alignment.mjs` | 0 差异 |
| B — i18n Key 完整性 | `scripts/validate-i18n-keys.mjs` | 0 missing |
| C — i18n 硬编码检测 | `scripts/validate-i18n-usage.mjs` | 0 违规 |
| D — 枚举 i18n 同步 | `scripts/build-enum-i18n.mjs --dry-run` | 无新枚举未同步 |
| E — 前后端 API 对齐 | `scripts/validate-frontend-api-alignment.mjs` | PATH NOT FOUND = 0, BODY FIELD mismatch = 0 |
| F — 架构契约校验（L2+） | `python scripts/probes/python_static.py` → `node .agents/skills/sfds/_shared/arch-contract/run.mjs --contract design/05-backend-architecture/data/arch-contract.js --facts scripts/probes/out/facts.json --report design/review/arch-contract/latest.json` | 退出码 0（未豁免 mechanical 违规 = 0） |

> 项目 `scripts/` 已有 `validate-frontend-api-alignment.mjs`（Vue 3 + FastAPI 版）；其余脚本若不存在，按 Review skill §0.2 规则生成后运行。任何不通过项 → 生成问题条目，修复后再进 Review。
> **裁剪规则（AUD-2 定案）**：校验项在目标项目**无对应资产**（如无 i18n/前端、无非架构项目）时，该项**按 PASS 处理**并在验证报告中注明「裁剪：无对应资产」；若资产存在但脚本缺失 → 生成脚本后运行，不得跳过。

**验证报告**：

```markdown
# 迭代报告：{变更简述}

> 日期：{YYYY-MM-DD} | 领域：{domain-slug}
> 模式：功能迭代 / 诊断修复 / 联调修正（批量） | 起点：§8.X | 问题数：{路径 C 时填写}
> API 相关：是 / 否

## 变更摘要
{一句话}

## 级联执行记录

| 步骤 | 负责 Skill | 变更摘要 | TDD 回归 | 结果 |
|------|-----------|---------|---------|------|
| §8.X | {skill} | {变更} | — | ✅ |
| ... | ... | ... | ... | ... |
| §8.5 | tdd-build | {新增/修改的用例} | ⚠️ | ✅ |
| §8.8 | tdd-execute | 全量测试 ({N} passed) | 含回归 | ✅ |
| Review | review | {受影响维度} | — | ✅ |

## TDD 回归用例（如有）
| 测试方法 | 覆盖场景 | 类型 | 状态 |
|---------|---------|------|------|
| {name} | {场景} | {类型} | ✅ |

## 验证结果
- [x] tdd-execute 全绿（{N}/{N} 通过）
- [x] Review 无新实质性问题
- [x] 变更意图已确认满足 / 原始问题已修复

## 相关文件变更
| 文件 | 变更类型 |
|------|---------|
| {文件路径} | 修改 / 新增 |
```

---

## 规则与守卫

| # | 规则 | 说明 |
|---|------|------|
| 0 | **强制两阶段** | 阶段一出计划 → ⛔ 停止等确认 → 阶段二执行。除非用户触发时声明合一步 |
| 1 | **变更起点必定位到步骤** | 每次迭代必须明确变更从 SFDS 哪个步骤（§8.X）开始 |
| 2 | **上溯铁律** | 定位插入点后，必须向上游追问：如果上游早就有这条规定，我还需要在这里改吗？答案"不需要"→ 起点上移 |
| 3 | **变更必级联** | 从起点开始，按依赖顺序级联执行全部下游步骤，不可跳过 |
| 4 | **API 相关变更必须追加 TDD 回归** | 起点在 §8.1/§8.2/§8.3/§8.3b/§8.5/§8.6 → §8.5 必须产出至少一个回归用例 |
| 5 | **设计是权威源** | 修正中如代码与设计冲突，以设计为准 |
| 6 | **委托不替代** | 每步实质性修改由对应专业 skill 执行，iterate 只做编排 |
| 7 | **域注册表同步** | 领域边界调整前先更新 `design/domain-registry.js` |
| 8 | **CHANGELOG 追踪** | 每步变更在对应目录的 `CHANGELOG.md` 中记录 |
| 9 | **原始输入存档** | 用户的需求澄清、决策、仲裁追加到 `design/01-raw-input/` |
| 10 | **全绿方可收敛** | 终点是 tdd-execute 全量通过 + Review 无新问题 |
| 11 | **问题清单闭环**（路径 C） | 批量修正以 FC1 问题清单为准绳：修复 + 测试覆盖每个问题 #，C3 验证时逐项确认，清单未清空不算完成 |
| 12 | **中文输出** | 所有分析、计划、报告用中文 |
| 13 | **修完自动启动服务** | 验证闭环后，默认启动 API 服务（从 AGENTS.md 读取启动命令） |
| 14 | **联调 Bug 必须同步自动化测试** | 联调（前后端对接/集成调试）中发现并修复的任何 Bug，必须同步加入自动化测试，**测试跑通才算问题解决**（"不写完测试不算修完"）。分类：API 问题（接口逻辑、数据校验、错误码映射）→ API 测试（`tests/` 目录，pytest）；页面问题（UI 渲染、交互逻辑、错误提示文案）→ Web UI 集成测试（Playwright/Selenium）。测试必须覆盖 Bug 场景（回归测试）和正常场景。此规则**无豁免**——联调 bug 修复不适用规则 #4 中"纯前端变更可豁免"的例外 |

---

## 场景速查

### 场景 1：加字段（路径 A）

> 用户："给订单表加一个 `cancelReason` 字段"

| 阶段 | 动作 |
|------|------|
| FA1 | 意图：给 Order 实体增加取消原因字段。类型：新增功能。涉及域：订单域 |
| FA2 | 表面插入点：§8.2（ER）或 §8.3（API）。先检查 ER |
| FA3 | 上溯：§8.2 → §8.1：工作流的"取消订单"节点有产生取消原因吗？有 → 插入点 §8.2；没有 → 插入点 §8.1 |
| C1 | 级联链：§8.2 → §8.3 → §8.4 → §8.5(⚠️新增回归) → §8.6+§8.7 → §8.8 → Review |
| ⛔ | **停止，等用户确认** |
| C2 | 逐步骤委托专业 skill 执行 |
| C3 | 测试全绿 + Review 通过 |

### 场景 2：改工作流（路径 A）

> 用户："用户注册流程要加手机验证步骤"

| 阶段 | 动作 |
|------|------|
| FA1 | 意图：注册工作流新增"手机验证"节点。类型：新增功能 |
| FA2 | 插入点：§8.1（工作流） |
| FA3 | 上溯：§8.1 → raw-input：原始需求里有手机验证的描述吗？有 → 插入点 §8.1；没有 → 先追加 raw-input |
| C1 | 级联链：§8.1 → §8.2 → §8.3 → §8.3b → §8.4 → §8.5(⚠️) → §8.6+§8.7 → §8.8 → Review |
| ⛔ | **停止，等用户确认** |
| C2-C3 | 级联执行 + 验证 |

### 场景 3：API 返回 500（路径 B）

> 用户："订单列表接口返回 500"

| 阶段 | 动作 |
|------|------|
| FB1 | 启动服务 → 复现请求 → 收集堆栈 |
| FB2 | 异常指向 NRE → 追问：代码行为与设计一致吗？一致 → §8.6（实现 bug）；不一致 → 继续上溯 |
| C1 | 起点 §8.6 → 级联：§8.6 → §8.5(⚠️新增回归) → §8.8 → Review |
| ⛔ | **停止，等用户确认** |
| C2-C3 | 级联执行 + 验证 |

### 场景 4：字段缺失（路径 B）

> 用户："API 响应里没有 `cancelReason` 字段"

| 阶段 | 动作 |
|------|------|
| FB1 | 查看响应体，确认缺失 |
| FB2 | DTO 中有这个属性吗？没有 → 向上：API 设计文档定义了吗？也没有 → 向上：ER 定义了吗？也没有 → **根因 §8.2** |
| C1 | 起点 §8.2 → 级联全链 |
| ⛔ | **停止，等用户确认** |
| C2-C3 | 逐步骤修正 + 验证 |

### 场景 5：需求驱动的连锁变更（路径 A）

> 用户："合同附录 v2 里说了要支持多设备同时绑定，现在的设计还是单设备的"

| 阶段 | 动作 |
|------|------|
| FA1 | 意图：设备绑定从 1:1 改为 1:N。来源：原始需求更新。类型：修改已有 |
| FA2 | 插入点：§8.1（工作流，"绑定设备"节点的业务规则要改） |
| FA3 | 上溯：§8.1 → raw-input：追加合同附录 v2 的相关内容 |
| C1 | 级联链：raw-input → §8.1 → §8.2 → §8.3 → §8.3b → §8.4 → §8.5(⚠️) → §8.6+§8.7 → §8.8 → Review |
| ⛔ | **停止，等用户确认** |
| C2-C3 | 全链级联 |

### 场景 6：联调批量修正（路径 C）

> 用户："联调发现 3 个问题：设备分组数量不对、搜索失效、地域分组语义不符，一起修一下"

| 阶段 | 动作 |
|------|------|
| FC1 | 收集清单：#1 device_count 恒为 0（期望=真实数量，实际=0）；#2 搜索失效（期望=按名称过滤，实际=前端未传参）；#3 地域分组语义（期望=按地域分组，实际=设计未定义地域维度）。与用户确认清单完整 |
| FC2 | #1 → B，根因 §8.6；#2 → B，根因 §8.7；#3 → A，根因 §8.2（上溯：工作流无地域分组规则 → 先补 raw-input） |
| FC3 | 合并计划：§8.2(#3) → §8.3(#3) → §8.5(回归×3) → §8.6(#1,#3) → §8.7(#2,#3) → §8.8 全量 → Review |
| ⛔ | **停止，等用户确认**（可调整单个问题的分类/根因） |
| C2 | 按合并计划执行，每步标注问题 # |
| C3 | tdd-execute 全绿 + 问题清单逐项闭环（3/3）+ Review 通过 |

---

## 完成检查清单

| 检查项 | 路径 A | 路径 B | 路径 C |
|--------|--------|--------|--------|
| 变更意图/问题现象已清晰描述 | ✅ | ✅ | ✅（问题清单已收集） |
| 变更起点已定位到具体 §8.X | ✅ | ✅ | ✅（每个问题已分类定位） |
| 上溯检查已完成（路径 A）/ 根因追溯链已记录（路径 B） | ✅ | ✅ | ✅（FC2 逐个完成） |
| API 相关判定已明确 | ✅ | ✅ | ✅ |
| 级联计划已输出 | ✅ | ✅ | ✅（FC3 合并计划） |
| ⛔ 阶段一停止，等待用户确认 | ✅ | ✅ | ✅ |
| 用户已确认计划 | ✅ | ✅ | ✅ |
| 每步变更通过对应 skill 执行 | ✅ | ✅ | ✅ |
| API 相关变更 → §8.5 回归用例已新增 | ✅ | ✅ | ✅（每个 API 问题至少 1 个） |
| 域注册表已同步（如需要） | ✅ | ✅ | ✅ |
| 每步 CHANGELOG 已更新 | ✅ | ✅ | ✅ |
| tdd-execute 全绿（含回归用例） | ✅ | ✅ | ✅ |
| Review 无新实质性问题 | ✅ | ✅ | ✅ |
| 变更意图已确认 / 原始问题已复现验证 | ✅ | ✅ | ✅（问题清单逐项闭环） |
| 迭代报告已输出 | ✅ | ✅ | ✅（批量报告） |
| 原始输入已存档（如有需求澄清） | ✅ | ✅ | ✅ |

---

## 与各专业 skill 的关系

| 步骤 | 委托的 Skill |
|------|------------|
| §8.1 工作流 | `business-workflow` |
| §8.2 ER | `entity-relationship` |
| §8.3 API 设计 | `api-design` |
| §8.3b 架构设计 | `backend-architecture-design` |
| §8.4 页面设计 | `client-ui-design`（移动/桌面统一） |
| §8.5 TDD | `tdd-build`（按端类型分发：Web→Stage 2，小程序→Stage 2b） |
| §8.6 API 代码 | `api-code-gen` |
| §8.7 前端代码 | `client-code-gen`（移动/桌面统一） |
| §8.8 测试执行 | `tdd-execute`（按端类型分发：Web→Stage 2，小程序→Stage 2b） |
| Review | `review` |
| 小程序运行时操作 | `wechatide-skill`（编译/预览/调试/自动化——由 `tdd-execute` 在 Stage 2b 中调用） |

> 本 skill 自身**不直接修改任何设计文件或代码**。所有实质性修改通过上表对应的专业 skill 执行。
> 本 skill 负责：判断改什么 → 计算改多少 → 编排执行顺序 → 守卫质量门控。

---

## 注意事项

1. **不是所有变更都需要全量级联。** 如果起点在 §8.6（纯代码修正，设计正确），级联只需 §8.6 → §8.5(⚠️ 回归) → §8.8 → Review。

2. **API 相关问题必须追加 TDD 回归（规则 #4）。** 这不是可选项。回归用例的价值在于记录"这个变更曾经发生过，以后不允许回归"。

3. **用户的需求澄清等同于 raw-input 变更。** 迭代过程中用户说"其实应该是这样……"→ 先存档到 `design/01-raw-input/`，再从 §8.1 开始级联。

4. **越早修正成本越低但影响面越大。** §8.1 改一行 → 下游全链重验。§8.6 改一行 → 只需重测。但如果在 §8.6 打补丁掩盖 §8.1 的设计缺陷，下次还会出问题。**本 skill 的价值就是阻止短视的局部修补。**

5. **路径 A 的上溯检查是防漏关键。** 很多迭代问题的根源是"用户说改 API，其实 ER 还没这个字段"——上溯检查就是防止这种"从半空中开始改"的情况。

6. **阶段一 ≠ 空谈。** 阶段一需要实际读文件、查代码、做上溯——不是"猜一个计划"。计划必须基于真实代码和设计现状。

7. **联调 Bug ≠ 一次性修补。** 联调中改的每个 Bug 都要落成自动化测试（规则 #14）：先判断问题类型（API / 页面），再写对应测试，跑通才算完。这条规则没有"纯前端豁免"——页面文案错了也要有 Web UI 集成测试兜底。

8. **联调 Bug 修复的级联计划中必须包含测试步骤。** 路径 B 的 C1 计划里，无论根因在哪一层，末尾的 §8.8 之前必须显式列出新增的回归/集成测试用例（对应 tdd-build 或测试代码编写），并在 C3 报告中记录"Bug 场景 + 正常场景"两类用例的执行结果。

9. **路径 C 的关键是清单闭环，不是一口气改完。** 联调批量修正的核心价值是把 N 个散乱问题整理成一份有序计划——先收集完整清单（FC1），再逐项分类定位（FC2），最后合并执行（FC3）。不要跳过 FC1 直接开修，遗漏问题会反复打断联调。

10. **路径 C 与 review 的分工。** 用户说"整体检查"时，先判断意图：只要检查报告 → 引导 `review`；检查后还要修 → 走路径 C（FC1 走查收集问题清单 → FC2 分类 → 修正闭环）。路径 C 是"检查 + 修正一体"，review 是"纯检查"。
