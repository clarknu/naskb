# MERGE-NOTES — iterate（仲裁版）

- 日期：2026-08-23 ｜ 底座：`inbox/zy-ai-consult/iterate/SKILL.md`（37bd78e52cf1）
- 三方关系：三方各异、逐字级同源。consult 比 zy-iot 多 C3.1-F 架构契约校验行及其脚注（底座已含，无需动作）；boxing 另有下述差异。

## 一、处置表

| # | 差异内容（来源） | 处置 | 理由 |
|---|----------------|------|------|
| B1 | 触发词「更新」归组：boxing 置于修正/调整命令组（修复之后），consult 置于新增意图组 | **回灌 R-002** | 纯技能内路由语义修正：「更新」按存量修改处理比按新增处理更符合语义；不含任何项目技术栈实例 |
| B2 | 派发表新增「"修复…"、"更新…" → 视范围而定（数量 1-2、单文件→默认 B；多端多文件→确认 A 或 C）」行，并将「更新…」移出存量修改→A 行 | **回灌 R-002** | 按数量分流的细粒度分发：避免小改动被重载路径 A 放大；引用的 A/B/C 均为本 skill 通用概念，明显通用且缺失，改动小 |
| B3 | 派发表将「不匹配/遗漏/未同步/对不上/接口对不上 → C（批量倾向检查）」拆为两行：单个/局部单点问题→B（诊断修复）、多个问题且源自联调→C（批量） | **回灌 R-002**（合并适配，见注） | 修正明显误路由：单点问题不应触发批量 C 走查。**合并适配披露**：采用 boxing 的 B/C 判定结构，但单点行保留 consult 的完整关键词枚举（含 未同步/接口对不上），否则这两个词失去派发表条目 |
| B4 | 触发词 `- 不匹配` 加行内注记 `# → 单点 B / 批量 C（见派发表）` | **回灌 R-002** | 随 B3 的派发表指引 |
| X1 | 「小程序运行时操作」行：boxing 先 `node tests/miniprogram-harness/bin/harness.js up <projectId>` 自动拉起（免扫码）再 wechatide-skill | **挂起：随 CASE-006 处理** | 关联 boxing 未提交改动（CASE-006），本次不回灌 |
| X2 | FB1 启动命令与规则 #13：boxing 读 `CLAUDE.md`，consult 读 `AGENTS.md` | 不回灌 | 技术栈实例，冲突以 consult 为准 |
| X3 | 级联链计算规则依据：boxing「SFDS §8」，consult「SFDS §8.0」 | 以 consult 为准 | 编号口径差异，微小 |
| X4 | boxing 独有注记「前端生成验收：code-gen 完成清单应包含调用 tdd-execute 执行对应端测试阶段；验证层技能是前端生成的验收方」（§8.7b 说明块后） | ✅已裁（R-007 保留回灌，2026-08-23，已泛化）：作为通用验收契约并入 C1 级联链说明块末尾（「前端生成验收契约」blockquote） | 内容疑似通用验收契约，但牵涉 code-gen/tdd-execute 技能族间契约归属；无法排除 consult 有意删除，提请仲裁庭裁决 |

R-002 定义：**按数量分流的细粒度分发**——触发词归组修正（B1）+ 派发表视范围分流（B2）+ 单点/批量拆分（B3/B4）。正文落点共 5 处小改。

## 二、三类清单

- **已回灌**：2 条裁决（R-002，含 B1-B4，落点 5 处编辑；R-007/X4 验收契约，落点 1 处编辑，已泛化）
- **待议**：0 项（X4 已随 R-007 于 2026-08-23 定案回灌）
- **挂起**：1 项（X1 harness 自动拉起 → 随 CASE-006 处理）

## 三、其他说明

- zy-iot 相对 consult 仅缺 C3.1-F 行——底座（consult）已含该行，属 consult 领先，无需处置。
- 未触碰：FB1/规则 #13 的 AGENTS.md 口径、C3.1-F 及其脚注、「与各专业 skill 的关系」表的 wechatide-skill 行（X1 挂起所致）。

## 四、R-007 回灌记录（2026-08-23）

- P-19 前端生成验收契约：在共享级联引擎 C1 的「关于小程序端」blockquote 后新增 1 段「前端生成验收契约」（code-gen 完成清单须含调用 tdd-execute 执行对应端测试阶段；验证层技能是前端生成的验收方），措辞已按端类型泛化、无实例绑定。
