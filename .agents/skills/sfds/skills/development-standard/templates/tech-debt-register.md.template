# 技术债登记册（Deferred Design Debt）—— `<项目名>`

> **用途**：登记"已识别、结论已定、但主动推迟执行"的设计/架构类技术债。每条必须完整记录
> **结论、暂缓原因、待还方向、触发条件**，做到"将来看到能直接还，而不用重新决策"。
> **区别于（分记，不混页）**：`docs/lessons-learned.md`（工程教训，事发后复盘）｜ `design/05-backend-architecture/data/arch-contract.js#knownDebts`（后端语义契约存量豁免）｜ `design/CHANGELOG.md`（按轮次的变更流水）。
> **铁律**：登记 ≠ 不做。登记只是把它**钉住**，明确"何时/在什么条件下还"。有归还动因时按"待还方向"执行，归还后移入"已还"。
> **有效性判据**：缺"待还方向/触发条件"的登记视为无效——将来无法直接还的登记没有意义。

---

## 登记表

| 编号 | 状态 | 登记日期 | 主题 | 归属层 |
|------|------|----------|------|--------|
| <!-- TD-001 --> | 🟡 暂缓（待还）/ 🟢 已还 / ⚪ 已失效（重新决策取代） | <!-- 日期 --> | <!-- 一句话主题 --> | <!-- 归属能力域/层，如 AI 工作流编排 / 后端架构 --> |

---

## TD-xxx｜<主题：一句话说清要还什么>

### 结论（已确认，但**暂不执行**）

> **主线不动**：<哪些既定决策继续走，不因本债动摇>。
> **要改的**：<明确只改什么、不改什么——收窄到最小可还单元>。

### 背景判断（为什么这么定）

<!-- 记录当时的判断依据：核对了什么代码/文档、选项对比、为什么选这条即 NOT 那条。将来还债的人靠这段恢复决策上下文。 -->

### 暂缓原因

<!-- 为什么现在不做：时机/依赖/成本/优先级…… -->

### 待还方向

<!-- 将来还债时具体怎么做：改哪个层、动哪些资产、步骤骨架。 -->

### 触发条件（满足即转待办）

<!-- 什么信号出现就该还：如"某依赖升级/某功能接入/第 N 次绕过同一问题"。 -->

### 状态流水

| 日期 | 状态 | 说明 |
|---|---|---|
| <!-- 登记日 --> | 🔴 暂缓 | <!-- 登记 --> |
