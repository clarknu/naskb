# MERGE-NOTES · sync-design-to-publish 泛化记录（CASE-009）

> 底座：inbox/zy-ai-consult@2026-08-23（v1.0.0，自声明"项目专属"）→ 泛化为 v1.1.0-arb.1
> 依据：R-005 方向裁决（需求方）+ CASE-009 立案

## 处置表

| 内容 | 原（项目专属） | 泛化后 | 处置 |
|---|---|---|---|
| 自声明"⚠️ 项目专属、不入全局" | 有 | 改为"通用方法论环节" | 转正 |
| 写死 Cloudflare 项目名 / 智能床路径 | `cloudflarePagesProject`、`--project-name <项目名>`、映射表硬编码 | 删除硬编码；项目差异外置为配置（project-slug / site / publish-dir） | 配置化 |
| 部署目标 | 单一 Cloudflare Pages | 统一"固定设计文档域名 + 单一终结点"，每项目一个节点 | 收敛终结点 |
| 映射表 | 本项目的 viewer→publish 映射 | 保留为**项目侧实例示例**（各项目按自身设计目录适配） | 示例化 |
| 凭证处理 | 已不硬编码（从环境变量读） | 保持 | 保留 |

## 待议 / 挂起

- 固定设计文档域名 + 路由到各项目节点的**托管基建**为独立工程项（基础设施，非技能可解），未含在本技能内。
- 映射表按"每个项目自己的 design/ 结构适配"，未强行统一。

## L1 复核

与底座 diff：frontmatter(+lineage/v1.1.0) + 顶部声明块 + 前置配置块 + 映射表标注。其余流程保持 config-driven 原文。
